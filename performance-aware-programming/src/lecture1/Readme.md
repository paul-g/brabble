To run the example and generate some plots:

```
bash matrix_mult.sh
```

__Note__ You should have gnuplot and gnuplot-x11 installed.
You can get these with (adapt for your package manager):

```
sudo apt-get install gnuplot gnuplot-x11
```
